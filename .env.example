# ===========================================
# Local Transcript App - Environment Config
# ===========================================
# Copy this file to .env and customize as needed
# cp .env.example .env

# ----- Ports -----
API_PORT=8000
WEB_PORT=3000

# ----- Upload Limits -----
MAX_UPLOAD_SIZE_MB=500
ALLOWED_EXTENSIONS=mp3,mp4,wav,m4a,webm,ogg,flac,mkv,avi,mov

# ----- Whisper Model -----
# Options: tiny, base, small, medium, large, large-v2, large-v3
# Smaller = faster but less accurate. 'small' is good balance.
WHISPER_MODEL=small

# Device: cpu or cuda (if GPU available)
WHISPER_DEVICE=cpu

# ----- YouTube Feature Flags -----
# YOUTUBE_SAFE_MODE: When true, only allows caption ingestion (no downloading)
# This is the recommended default for compliance.
YOUTUBE_SAFE_MODE=true

# YOUTUBE_AUTO_INGEST: When true, enables automatic audio extraction from URLs.
# WARNING: This may violate platform terms of service. Use at your own risk.
# Only enable if you understand the implications.
YOUTUBE_AUTO_INGEST=false

# Maximum video duration allowed for YouTube ingestion (in minutes)
YOUTUBE_MAX_DURATION_MIN=120

# ----- Database -----
# SQLite path (relative to container, not host)
DATABASE_URL=sqlite:///data/db/transcript.db

# ----- Storage Paths -----
# These are container paths. Mapped via docker-compose volumes.
UPLOADS_DIR=/data/uploads
OUTPUTS_DIR=/data/outputs

# ----- Logging -----
LOG_LEVEL=INFO

# ----- Development -----
# Set to 'development' for hot reload and debug mode
ENVIRONMENT=production
DEBUG=false
